// Enables the "Enter the Undermurk!" debrief track.
const UNDERMURK = true;

// Active Undermurk entry (true) vs locked "Coming Soon" (false) in the global
// menu and on the debrief minigame tile.
const UNDERMURK_BUTTON = false;

// Controls the floating global-menu button
const SHOW_GLOBAL_MENU_BUTTON = true;

// Shows a "Whole Class" option in the Number of Players setup tabs.
const WHOLE_CLASS = true;

// Splash code-entry keypad: true = A–Z plus digits (share codes on touch devices);
// false = digits 1–9 only (legacy purchase codes).
const ALPHABET_KEYBOARD = false;
